/*package desafio01;

import java.util.Scanner;

public class Multiplicacao {
    static int multiplicacao(Double a , double b) {
        double b = new double[9];
        double res = a * b;

        return (a + " * " + b " = " + res)
    }
    public static void main(String[] args) {
        Scanner valor = new Scanner(System.in);
        System.out.println("Insira o valor do multiplicando:");
        Double a = valor.nextDouble();
        System.out.println("Insira o primeiro numero referente ao intervalo do multiplicador:");
        Double[] b = valor.nextDouble();
        for (Double[] : (b[0] < 0; b[0] > 3000));
        {
            System.out.println("Erro, a regra de negócio não permite este valor");
        }

        System.out.println("Insira o segundo numero referente ao intervalo do multiplicador:");
        Double[] b 9] = valor.nextDouble();
        if (b[9] <= 0 || b[9] > 3000);
        {
            System.out.println("Erro, a regra de negócio não permite este valor");

        }else {
                if (a <=0 || a > 3000){
                    System.out.println("Erro, a regra de negócio não permite este cálculo");
        } else {
                if ((b[9] - b[0]) > 10 ){
                   System.out.println("Erro, a regra de negócio não permite este cálculo");
        }else {
                if (b[9] < b[0]){
                    System.out.println("Erro, a regra de negócio não permite este cálculo");
                }else
                    return;
                }
        }
    }
}}
*/